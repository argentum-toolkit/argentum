{{ upper_camel typeName }}Input {
    user_name: {{name}}_value(),
    violations: {{name}}_violations,
    oninput: move |event: {{ upper_camel typeName }}| {
        {{name}}_value.set(event);
        update();
    }
}
