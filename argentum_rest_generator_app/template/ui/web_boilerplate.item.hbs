use argentum_standard_infrastructure::invariant_violation::ViolationsDto;
use crate::dto::response::{
{{#each responseNames as |response|}}
    {{response}}Response,
{{/each}}
};

{{# each dependencies as |dep|}}
use {{dep}};
{{/each}}

use dioxus::prelude::*;
use crate::user_account::service::ClientSideAuthenticator;
use argentum_rest_infrastructure::data_type::{
    AuthHeaderParams, EmptyQueryParams, HttpParams, HttpRequest,
};
use argentum_standard_infrastructure::invariant_violation::ViolationItemDto;
use crate::client::Client;
use crate::dto::operation_response_enum::{{upper_camel operation.operationId}}OperationResponseEnum;
use crate::dto::params::{{upper_camel operation.operationId}}Params;
use crate::dto::path_params::{{upper_camel operation.operationId}}PathParams;
use crate::dto::request::{{upper_camel operation.operationId}}Request;
{{!-- use crate::dto::response::Status400Response; --}}
{{!-- use crate::dto::response::Status401Response; --}}
use crate::dto::schema::LoginWithPasswordSchema;
use crate::dto::schema::ProblemDetail;

use crate::ui::form_data::{{upper_camel operation.operationId}}FormData;

use crate::ui::form_data::{{upper_camel operation.operationId}}Props;
use dioxus::prelude::*;

fn extract_body_violations_open_api_problem_details(
    problem: ProblemDetail,
) -> Option<ViolationsDto> {
    match problem.body {
        Some(body_violation) => {
            let pp = serde_json::to_string(&body_violation).unwrap();
            let violations: ViolationsDto = serde_json::from_slice(pp.as_ref()).unwrap();
            Some(violations)
        }
        None => None,
    }
}

pub fn create_{{snake operation.operationId}}_web_boilerplate(
    server_url: String,
    props: {{upper_camel operation.operationId}}Props,
) -> (impl FnMut(Event<FormData>), {{upper_camel operation.operationId}}FormData) {
    let mut form_data = {{upper_camel operation.operationId}}FormData::new();

    let on_submit = {
        let authenticator = use_context::<Signal<ClientSideAuthenticator>>();

        move |_| {
            spawn(async move {
                // rsx_violations.clear();
                form_data.violations.email.set(None);
                form_data.violations.password.set(None);
                form_data.disabled.set(true);
                form_data.errors.set(vec![]);

                let client =
                    Client::new(server_url, "/api/v1".to_string());

                let req = {{upper_camel operation.operationId}}Request::new(
                    LoginWithPasswordSchema::new(
                        (form_data.values.email)(),
                        (form_data.values.password)(),
                    ),
                    {{upper_camel operation.operationId}}Params::new(
                        {{upper_camel operation.operationId}}PathParams::new(),
                        EmptyQueryParams {},
                        AuthHeaderParams::new(authenticator().anonymous_token().unwrap()),
                    ),
                );

                let res = client.user_logins_with_password(req).await;

                match res {
                    Ok(data) => match data {
                        {{upper_camel operation.operationId}}OperationResponseEnum::Status200(r) => {
                            props.on_user_logged_in_successfully.call(r);
                        }
                        {{upper_camel operation.operationId}}OperationResponseEnum::Status400(r) => {
                            match r.clone() {
                                Status400Response::ApplicationProblemJson(j) => {
                                    let body_violations =
                                        extract_body_violations_open_api_problem_details(j.0);

                                    if let Some(violations) = body_violations {
                                        if let Some(ViolationItemDto::Object(items)) =
                                            violations.items
                                        {
                                            form_data
                                                .violations
                                                .email
                                                .set(items.get("email").cloned());
                                            form_data
                                                .violations
                                                .password
                                                .set(items.get("password").cloned());
                                        };
                                    };

                                    form_data.disabled.set(false);
                                }
                            }

                            props.on_status_400.call(r);
                        }

                        {{upper_camel operation.operationId}}OperationResponseEnum::Status401(r) => {
                            match r.clone() {
                                Status401Response::ApplicationProblemJson(j) => {
                                    let body_violations =
                                        extract_body_violations_open_api_problem_details(j.0);

                                    if let Some(violations) = body_violations {
                                        if let Some(ViolationItemDto::Object(items)) =
                                            violations.items
                                        {
                                            form_data
                                                .violations
                                                .email
                                                .set(items.get("email").cloned());
                                            form_data
                                                .violations
                                                .password
                                                .set(items.get("password").cloned());
                                        };
                                    };

                                    form_data.disabled.set(false);
                                }
                            }
                            props.on_status_401.call(r);
                        }
                    },
                    Err(e) => {
                        form_data.disabled.set(false);
                        props.on_error.call(e);
                    }
                }
            });
        }
    };

    (on_submit, form_data)
}
