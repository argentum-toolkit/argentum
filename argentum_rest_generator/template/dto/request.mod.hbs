{{#each operations as |operation|}}
{{~#if operation.request~}}
mod {{snake operation.id}}_request;
{{/if}}
{{/each}}

{{#each operations as |operation|}}
{{~#if operation.request~}}
pub use {{snake operation.id}}_request::{{operation.id}}Request;
{{/if}}
{{/each}}
