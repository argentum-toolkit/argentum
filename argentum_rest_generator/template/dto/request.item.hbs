use argentum_rest_infrastructure::data_type::HttpRequest;
use crate::dto::schema::{{ operation.id }}Params;
use {{operation.request.body_schema}};

pub struct {{ operation.id }}Request {
    pub body: {{trim_mod operation.request.body_schema}},
    pub params: {{ operation.id }}Params,
}

impl HttpRequest for {{ operation.id }}Request {
    type Body = {{trim_mod operation.request.body_schema}};
    type Params = {{ operation.id }}Params;

    fn new(body: {{trim_mod operation.request.body_schema}}, params: {{ operation.id }}Params) -> Self {
        Self { body, params }
    }

    fn body(&self) -> &Self::Body {
        &self.body
    }

    fn params(&self) -> &Self::Params {
        &self.params
    }
}
